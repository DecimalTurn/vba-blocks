There can be advantages to transform VBA source code before building it into a VBA-Enabled Document.

For instance, you could want to include boilerplate code at the start of each of your functions to make it easier to get a call stack that is visible in VBE. eg: https://github.com/sancarn/stdVBA-Compiler


